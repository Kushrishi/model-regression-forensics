name: Exp009 Stage A

on:
  push:
    branches: ["main"]
    paths:
      - "experiments/009_stochastic_counterfactual_certification/STAGE_A_RUN_REQUEST.json"
permissions:
  contents: read

concurrency:
  group: exp009-stage-a
  cancel-in-progress: false

jobs:
  train:
    name: t${{ matrix.trajectory }} / ${{ matrix.state }}
    runs-on: macos-15
    timeout-minutes: 355
    strategy:
      fail-fast: false
      max-parallel: 6
      matrix:
        trajectory: [0, 1, 2]
        state: [baseline, composite]

    steps:
      - uses: actions/checkout@v7.0.1

      - uses: actions/setup-python@v7.0.0
        with:
          python-version: "3.12"

      - uses: astral-sh/setup-uv@v10.2.0
        with:
          enable-cache: true

      - name: Install research dependencies
        run: uv sync --extra research --extra dev

      - name: Verify frozen state preflight
        run: |
          uv run python scripts/run_exp009_mps_certification_pilot.py \
            --mode preflight \
            --trajectory-id "${{ matrix.trajectory }}" \
            --state "${{ matrix.state }}"

      - name: Record hosted-runner provenance
        run: |
          {
            echo "runner_os=${RUNNER_OS}"
            echo "runner_arch=${RUNNER_ARCH}"
            echo "image_os=${ImageOS:-unknown}"
            echo "image_version=${ImageVersion:-unknown}"
            echo "uname=$(uname -a)"
            echo "macos=$(sw_vers -productVersion)"
            echo "cpu=$(sysctl -n machdep.cpu.brand_string)"
            echo "memory_bytes=$(sysctl -n hw.memsize)"
          } > runner_provenance.txt

      - name: Train frozen Stage-A state
        run: |
          uv run python scripts/run_exp009_mps_certification_pilot.py \
            --mode train \
            --trajectory-id "${{ matrix.trajectory }}" \
            --state "${{ matrix.state }}"

      - name: Build truth-isolated attribution inputs
        if: matrix.state == 'composite'
        run: |
          run_id=$(printf "mps_v2_t%04d_composite_e7_bs32_lr2e5" "${{ matrix.trajectory }}")
          run_dir="artifacts/exp009/mps_certification_pilot_v2/runs/${run_id}"
          uv run python scripts/build_exp009_attribution_bundle.py             --output-dir "${run_dir}/attribution_bundle"

      - name: Score composite with frozen Grad-Dot baseline
        if: matrix.state == 'composite'
        run: |
          run_id=$(printf "mps_v2_t%04d_composite_e7_bs32_lr2e5" "${{ matrix.trajectory }}")
          run_dir="artifacts/exp009/mps_certification_pilot_v2/runs/${run_id}"
          uv run python scripts/run_exp009_graddot_attribution.py             --trajectory-id "${{ matrix.trajectory }}"             --checkpoint-dir "${run_dir}/model"             --bundle-dir "${run_dir}/attribution_bundle"             --output "${run_dir}/graddot_summary.json"
          rm -f "${run_dir}/attribution_bundle/composite_release.jsonl"

      - name: Package portable evidence
        shell: bash
        run: |
          run_id=$(printf "mps_v2_t%04d_%s_e7_bs32_lr2e5" "${{ matrix.trajectory }}" "${{ matrix.state }}")
          run_dir="artifacts/exp009/mps_certification_pilot_v2/runs/${run_id}"
          cp runner_provenance.txt "${run_dir}/runner_provenance.txt"
          rm -rf "${run_dir}/model"
          shasum -a 256 "${run_dir}/train_summary.json" "${run_dir}/development_eval_logits.jsonl"
          cat "${run_dir}/train_summary.json"

      - name: Upload portable state evidence
        uses: actions/upload-artifact@v7.0.1
        with:
          name: exp009-stage-a-t${{ matrix.trajectory }}-${{ matrix.state }}
          path: artifacts/exp009/mps_certification_pilot_v2/runs/
          if-no-files-found: error
          retention-days: 30

  analyze:
    name: Frozen Stage-A gate
    needs: train
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v7.0.1

      - uses: actions/setup-python@v7.0.0
        with:
          python-version: "3.12"

      - uses: astral-sh/setup-uv@v10.2.0
        with:
          enable-cache: true

      - name: Install analysis dependencies
        run: uv sync --extra dev

      - name: Download all Stage-A evidence
        uses: actions/download-artifact@v8.0.1
        with:
          pattern: exp009-stage-a-*
          path: artifacts/exp009/mps_certification_pilot_v2/runs
          merge-multiple: true

      - name: Apply frozen Stage-A gate
        run: |
          uv run python scripts/analyze_exp009_mps_stage_a.py
          cat artifacts/exp009/mps_certification_pilot_v2/stage_a_analysis.json

      - name: Aggregate blind Grad-Dot ranking
        run: |
          uv run python scripts/analyze_exp009_graddot_stage_a.py             --runs-root artifacts/exp009/mps_certification_pilot_v2/runs             --output artifacts/exp009/mps_certification_pilot_v2/graddot_aggregate.json
          cat artifacts/exp009/mps_certification_pilot_v2/graddot_aggregate.json

      - name: Evaluate already-written ranking against benchmark truth
        run: |
          uv run python scripts/score_exp009_graddot_truth.py             --ranking artifacts/exp009/mps_certification_pilot_v2/graddot_aggregate.json             --output artifacts/exp009/mps_certification_pilot_v2/graddot_truth_evaluation.json
          cat artifacts/exp009/mps_certification_pilot_v2/graddot_truth_evaluation.json

      - name: Upload Stage-A analysis
        uses: actions/upload-artifact@v7.0.1
        with:
          name: exp009-stage-a-analysis
          path: |
            artifacts/exp009/mps_certification_pilot_v2/stage_a_analysis.json
            artifacts/exp009/mps_certification_pilot_v2/graddot_aggregate.json
            artifacts/exp009/mps_certification_pilot_v2/graddot_truth_evaluation.json
          if-no-files-found: error
          retention-days: 90
